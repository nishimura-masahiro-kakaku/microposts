<%= form_for(current_user.favorite_posts.build, remote: true) do |f| %>
  <%= hidden_field_tag :post_id, micropost.id %>
  <%= image_submit_tag("unfavorite.png", class: 'favorite_button', alt: 'Favorite') %>
<% end %>